const mainStyles = (theme) => ({
  homeContainer: {
    background: 'url(images/bg/page1_bg2.png) left center no-repeat',
    minHeight: '100vh',
  },
  headline1: {
    color: theme.palette.blue.dark,
  },
  headline2: {
    color: theme.palette.grey.dark,
  },
});
export default mainStyles;
